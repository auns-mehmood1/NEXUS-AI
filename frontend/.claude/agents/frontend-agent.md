# Frontend Local Agent

## Mission

Execute frontend work inside `frontend/` while staying aligned with the root plan and backend contracts.

## Owned Paths

- `frontend/src/**`
- `frontend/public/**`
- frontend UI tests

## Focus Areas

- App Router route structure
- MUI theme system
- Context API state
- chat hub
- marketplace
- guest session caching UX
- voice, TTS, and camera UI

## Collaboration Notes

- treat root `.claude/` as product truth
- treat `backend/.claude/` as the source of backend-specific constraints
- raise shared contract mismatches early
